#
# File: .github/workflows/immortalwrt-sdk-trigger.yml
# Description: Trigger SDK build when a version bump PR is merged
#
# Copyright (c) 2024-2025 SuperKali <hello@superkali.me>
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.

name: Trigger SDK Build on Version Bump

on:
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - 'config/*/version.json'

jobs:
  trigger-sdk:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Detect bumped tracks and trigger SDK builds
      env:
        GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      run: |
        # Trigger SDK build for each active track that was changed
        for vf in config/*/version.json; do
          status=$(jq -r '.status' "$vf")
          [ "$status" != "active" ] && continue

          tracks=$(jq -r '.tracks[]' "$vf")
          for track in $tracks; do
            echo "Triggering SDK build for track: $track"
            gh workflow run immortalwrt-sdk-matrix-builder.yml \
              -f release_type="$track" \
              -f architecture=ALL
          done
        done
